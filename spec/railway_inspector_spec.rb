require "spec_helper"

describe RailwayInspector do
  it "has a version number" do
    expect(RailwayInspector::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
